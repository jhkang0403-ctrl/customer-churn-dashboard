# 나의 분석 워크플로우 (초안) — 상담 재문의 분석

> 적용 대상 데이터: **상담이력 데이터** (`data_messy_상담이력.xlsx`, 2025-01/02) + BigQuery `project1_day1.consultations` (2024-01~2025-02, 1,530건)
> 작성일: 2026-08-01

---

## 1. Define — 이 데이터로 답하고 싶은 질문

- **채널별·문의유형별 재문의율은 어떻게 다르며, 어떤 조합이 특히 재문의가 잦은가?** (이번 프로젝트의 핵심 질문)
- (다음 프로젝트 후보로 보류) 그 차이가 실제 고객 이탈로도 이어지는가?
- (다음 프로젝트 후보로 보류) 2025년 신규 2개월 데이터가 2024년 패턴과 다른 추세를 보이는가?
- (다음 프로젝트 후보로 보류) 상담시간이 길수록 재문의가 잦아지는가?

---

## 2. Acquire — Part 1에서 판단한 확보 경로와 이유

- **신규 확보**: `data_messy_상담이력.xlsx` (2025-01·02월, 팀별 시트) — 담당자가 월별로 직접 관리하는 원본 상담 기록. 병합 셀·소계 행·색상 표시가 섞여 있어 그대로는 분석에 못 씀.
- **기존 보유**: BigQuery `project1_day1.consultations`, `data/data_consultations.csv` — 2024년 1,320건이 이미 존재. 정제한 2025년치를 같은 스키마로 이어붙임(consult_id CON1321~CON1530).
- **이번 질문에 필요한 추가 확보는 없음**: 질문에 필요한 필드(`channel`, `category`, `is_recontact`)가 이미 통합된 1,530건 안에 모두 있어, 별도 데이터 확보 절차 없이 바로 Model 단계로 진행 가능.

---

## 3. Profile — 이 데이터에 있을 것 같은 문제

- **병합 셀**: 있음 — `소속`(팀) 컬럼이 팀 단위로 세로 병합
- **소계 행**: 있음 — `상담원`에 `"n팀 소계"` 형태로 섞여 있음
- **텍스트 숫자**: 있음 — `상담시간`이 `"16분"`(문자열)과 `33`(순수 숫자)으로 혼재
- **날짜 형식 혼재**: `상담일`이 `YYYY-MM-DD`, `YY.M.D`, 엑셀 일련번호(raw) 세 가지로 혼재
- **색상 인코딩**: `재문의여부`가 텍스트(`N`)와 색(연분홍, 텍스트 없음) 두 방식으로 이중 인코딩되어 있어 pandas만으로는 일부 정보가 유실됨

---

## 4. Confirm — 이 데이터에서 한 행이 무엇을 의미할지, 확실하지 않은 부분

- **한 행의 의미**: 월 × 팀 × 상담원 × 상담일 × 고객ID 단위의 "상담 1건" 기록
- 확실하지 않았던 점 1: 소계 행 구분 — `상담원`에 `"소계"` 포함 여부로 판단
- 확실하지 않았던 점 2 (가장 중요): **`처리결과`(상담 종료 시점 분류)와 `재문의여부`(사후 실제 재문의)가 같은 개념인지** — 교차 확인 결과 서로 독립적임을 확인 (`처리결과=재문의`인데 `재문의여부=N`인 행이 실제로 존재, 반대의 경우도 존재). 이후 모든 재문의율 계산은 `재문의여부` 기준으로 통일.
- 확실하지 않았던 점 3: 재문의여부의 색상(텍스트 없음+연분홍)이 정확히 무엇을 뜻하는지 — 색칠된 셀 개수가 소계 행의 재문의 건수 합과 정확히 일치함을 근거로 "재문의 있음(True)"으로 확정

---

## 5. Clean — 오늘 배운 정제 기법 중 적용하게 된 것

- **병합 셀 → forward fill**: `소속` 컬럼을 그룹 내 위 값으로 채움
- **소계 행 → 교차검증 후 제거**: 처리결과 소계(그룹 총 건수)·재문의여부 소계(그룹 내 True 건수)가 실제 집계와 일치하는지 검증(6개 그룹 전부 일치) 후 제거
- **텍스트 숫자 → 숫자 변환**: 상담시간을 분 단위 정수로 통일
- **날짜 3종 혼재 → ISO 통일**: `YYYY-MM-DD`/`YY.M.D`/엑셀 일련번호를 모두 표준 형식으로 변환, 변환 결과가 시트월과 일치하는지 검증
- **색상 → 불리언 변환**: 재문의여부를 `True`/`False`로 변환 (텍스트 `N`→False, 텍스트없음+연분홍→True)

---

## 6. Model — 내 데이터로 계산하고 싶은 지표

- **재문의율** = 재문의(True) 건수 ÷ 전체 상담 건수 × 100
- 이를 세 가지 레벨로 계산: ① 채널별, ② 문의유형별, ③ **채널×문의유형 조합별** (질문의 핵심)

---

## 7. Analyze — 그 지표로 확인한 결과

- **채널별**: 이메일 36.2% > 채팅 26.0% > 앱 24.6% > 전화 12.1% — 전화와 이메일 사이에 3배 차이
- **문의유형별**: 20.0%~23.6% 범위로 채널만큼 차이가 크지 않음 (기타 23.6% ~ 명의변경 20.0%)
- **채널×문의유형 조합(24개 셀)**:
  - 가장 잦은 조합: **이메일×부가서비스 40.7%**(n=27), 이메일×장애신고 39.1%(n=46), 이메일×요금문의 38.3%(n=60)
  - 가장 드문 조합: **전화×해지문의 8.9%**(n=146)
  - **이메일 채널은 거의 모든 문의유형에서 상위권을 차지** — 문의유형보다 채널 자체가 재문의율에 더 강하게 관여하는 변수라는 채널별 분석 결론이 조합 수준에서도 재확인됨

---

## 8. Validate — 결과를 믿기 전에 확인한 것

- 채널별(최소 138건)·문의유형별(최소 130건) 표본은 모두 충분
- **조합(24칸) 표본은 편차가 큼**: 최대 192건(전화×요금문의) ~ 최소 8건(앱×기타)
- 표본 20건 미만 조합 3개(**앱×기타 n=8, 앱×명의변경 n=11, 이메일×명의변경 n=16**)는 재문의율이 높게/낮게 튈 수 있어 "특히 위험한 조합" 결론의 근거로는 사용하지 않고 참고용으로만 표시
- 전체 재문의율(21.5%, 1,530건 기준) 대비 위 조합별 수치의 편차를 해석 기준으로 삼음

---

## 9. Publish — 결과를 어떤 형태로 보여줄 것인가

- **대시보드**: `customer-churn-dashboard` Streamlit 앱에 "상담 재문의 분석" 페이지로 신규 추가 — KPI(전체 재문의율/표본수/채널 격차), 채널별·문의유형별 막대, 채널×문의유형 히트맵(표본 부족 조합 표시 포함)
- 데이터가 월별로 계속 쌓이므로 정적 리포트보다 대시보드 형태가 적합 — `data_consultations.csv` 기준으로 구성해 다음 달 데이터가 추가되면 자동 반영

---

## 10. Approve & Send

아직 확정 아님 — 다음 단계에 작성
